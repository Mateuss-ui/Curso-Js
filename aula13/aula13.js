let contador = 1;

while (contador <= 6) {  // PRIMEIRA TESTA DEPOISE EXECUTA O BLOCO 
    console.log(`seu contador está em ${contador}`);
    contador++    
}
let contador2 = 1; // Executa o bloco depois testa
do {
    console.log(`seu contador está em ${contador2}`);
    contador2++
} while (contador2 <= 6)
